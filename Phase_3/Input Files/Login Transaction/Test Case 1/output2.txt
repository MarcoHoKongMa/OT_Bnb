Output:
	Invalid: Transaction failed, must first login

Output:
	Invalid: Transaction failed, must first login

Output:
	Invalid Transaction failed, must first login

Output:
	Invalid Transaction failed, must first login

Output:
	Invalid Transaction failed, must first login

Output:
	Invalid Transaction failed, must first login

Output:
	Valid: Transaction successful
